# webwrap

Web Wrap is a cross-platform C library and CLI that provides runtime-loaded HTTP client and server support through native platform backends.

## Layout

- `include/webwrap/`: public headers
- `src/`: library and CLI sources
- `tests/`: test suite

## Build

```sh
make build
```

## Test

```sh
make test
```
